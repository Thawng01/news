import "./loading.css";

const Loading = () => {
    return (
        <div className="loading">
            <div className="spinner-border text-primary" />
        </div>
    );
};

export default Loading;
